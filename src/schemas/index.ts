import { 
    moviesCreateSchema, 
    moviesPaginationSchema, 
    moviesReadSchema, 
    moviesSchema, 
    moviesSortSchema, 
    moviesUpdateSchema 
} from "./movies.schemas";

export {
    moviesSchema,
    moviesCreateSchema,
    moviesReadSchema,
    moviesUpdateSchema,
    moviesSortSchema,
    moviesPaginationSchema,
}